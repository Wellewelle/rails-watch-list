<h1><%= @list.name %></h1>
<br>
<%= cl_image_tag @list.photo.key, height: 300, width: 400, crop: :fill %>

<ul>
  <% @list.movies.each do |movie| %>
    <li>
    <%= movie.title %>
    </li>
  <% end %>
</ul>
<%# <%=%>
<%= link_to "Back to list", lists_path %>
<br>
<%= link_to "Add movie to list", list_bookmarks_new_path(@list) %>
